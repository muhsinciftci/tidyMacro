% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fSignRestr.R
\name{fSignRestr}
\alias{fSignRestr}
\alias{print.fSignRestr}
\title{Sign, Narrative and Sign-Plus-IV Identification of a Structural VAR}
\usage{
fSignRestr(
  y,
  p,
  c = 1,
  sign,
  nsteps = 40,
  ndraws = 500,
  sr_hor = 1,
  sr_rot = 500,
  max_post_draws = 1000,
  conf = 90,
  inference = 1,
  narrative = NULL,
  dates = NULL,
  instrument = NULL,
  narr_weight_mc = 0,
  resid_from_draw = FALSE,
  store_draws = TRUE,
  exog = NULL,
  varnames = NULL,
  n_threads = 0,
  seed = 42,
  verbose = FALSE
)

\method{print}{fSignRestr}(x, ...)
}
\arguments{
\item{y}{A T x N numeric matrix (or data frame) of endogenous variables.}

\item{p}{Integer lag order.}

\item{c}{Integer intercept indicator: 1 to include an intercept (default), 0
to exclude it.}

\item{sign}{An N x ds matrix of sign restrictions: \code{+1} the response must
be non-negative, \code{-1} non-positive, \code{0} unrestricted. \code{ds}
must equal N when no instrument is used, and N - k with a k-column
instrument (\code{k = ncol(instrument$Z)}), since shocks 1:k are the
instrumented ones.}

\item{nsteps}{Integer number of horizons to report, impact included
(default 40).}

\item{ndraws}{Integer number of accepted draws (default 500).}

\item{sr_hor}{Integer. Restrictions are imposed at horizons
\code{0, ..., sr_hor - 1} (default 1, impact only).}

\item{sr_rot}{Integer maximum rotations attempted per parameter draw
(default 500).}

\item{max_post_draws}{Integer maximum parameter draws per accepted draw
(default 1000).}

\item{conf}{Numeric coverage of the credible bands, in percent (default 90;
Uhlig (2005) and Antolin-Diaz and Rubio-Ramirez (2018) report 68, so pass
\code{conf = 68} to match them).
A vector requests several levels at once, e.g. \code{c(68, 90)}: the first
is reported in \code{IRinf}/\code{IRsup} and every level is returned in
\code{bands}. Extra levels do not require storing the draws.}

\item{inference}{Integer: 1 (default) draws reduced-form parameters from the
posterior; 0 holds them at OLS, leaving only rotation uncertainty.}

\item{narrative}{Optional list of narrative restrictions with components
\code{sign} (a list of \code{shock}, \code{period}, \code{sign}) and
\code{dom} (a list of \code{shock}, \code{period}, \code{var}). Periods may
be integer rows of \code{y} or, when \code{dates} is supplied, date strings
or \code{Date} values.}

\item{dates}{Optional vector of length \code{nrow(y)} — character labels or
\code{Date} values — used to resolve narrative periods given as dates
rather than as row numbers.}

\item{instrument}{Optional list with component \code{Z}, a T x k instrument
matrix aligned with the rows of \code{y}, missing values allowed. Selects
the \code{sign+iv} scheme. With \code{k = 1} the single shock is identified
by \code{\link{fRecoverBIV_cpp}} (Gertler-Karadi/Mertens-Ravn); with
\code{k > 1} the k shocks are jointly identified by
\code{\link{fRecoverBIVMulti_cpp}}, following the block generalisation in
\code{VARiriv_B.m} (Cesa-Bianchi & Sokol 2022). Either way the identified
column(s) are held at their OLS value across draws, as the VAR Toolbox
does, so the bands do not carry the instrument's own sampling uncertainty.}

\item{narr_weight_mc}{Integer. Positive values switch on the ADRR importance
reweighting, using this many Monte Carlo replications per draw. \code{0}
(default) uses plain rejection sampling, as the VAR Toolbox does.}

\item{resid_from_draw}{Logical. \code{FALSE} (default) evaluates narrative
restrictions on OLS residuals, matching the VAR Toolbox; \code{TRUE} uses
the residuals implied by each parameter draw.}

\item{store_draws}{Logical; keep the full IRF and FEVD draw distributions
(default TRUE).}

\item{exog}{Optional T x M matrix of exogenous regressors.}

\item{varnames}{Optional character vector of length N.}

\item{n_threads}{Integer; 0 (default) uses all available OpenMP threads.}

\item{seed}{Integer base seed (default 42).}

\item{verbose}{Logical; print progress diagnostics (default FALSE).}

\item{x}{An object of class \code{"fSignRestr"}.}

\item{...}{Ignored.}
}
\value{
An object of class \code{"fSignRestr"}: the list returned by
  \code{\link{fSR_cpp}}, with \code{IRall} and \code{VDall} reshaped to
  \code{c(N, N, nsteps, ndraws)} and with \code{var}, \code{varnames},
  \code{ident}, \code{p}, \code{c}, \code{nsteps} and \code{iv} attached.
}
\description{
Front-end for \code{\link{fSR_cpp}}. Estimates a VAR, then identifies it by
sign restrictions, optionally combined with Antolin-Diaz and Rubio-Ramirez
narrative restrictions and with an external instrument that pins down the
first structural impact column. Inference is Bayesian throughout: reduced-form
parameters are drawn from their flat-prior Normal-inverse-Wishart posterior and
each draw is paired with a Haar-uniform rotation.
}
\details{
Impulse responses are indexed \code{[variable, shock, horizon]} and FEVD
shares are on the \code{[0, 1]} scale.

With a k-column instrument, the first column of \code{sign} corresponds to
structural shock k + 1: shocks 1:k are the instrumented ones and are not
sign-restricted.
}
\section{Methods (by generic)}{
\itemize{
\item \code{print(fSignRestr)}: Print a compact summary of the identified model.

}}
\references{
Uhlig, H. (2005). \emph{Journal of Monetary Economics}, 52(2), 381--419.

Antolin-Diaz, J., & Rubio-Ramirez, J. F. (2018). \emph{American Economic
Review}, 108(10), 2802--2829.
}
\seealso{
\code{\link{fSR_cpp}}, \code{\link{fPlotIRFSign}},
  \code{\link{fRecoverBIV_cpp}}, \code{\link{fRecoverBIVMulti_cpp}},
  \code{\link{fHDShock_cpp}}
}
