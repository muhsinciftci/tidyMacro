% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{fheteroIRF}
\alias{fheteroIRF}
\title{Heteroskedasticity-Based VAR Identification (Proxy IV, Treatment Months)}
\usage{
fheteroIRF(var_result, Z, adjustu, indsR1, hor, nvar, scale = 10)
}
\arguments{
\item{var_result}{List returned by \code{fVAR()}.}

\item{Z}{Proxy matrix (T1 x 1), aligned to the identification window
defined by \code{adjustu}.}

\item{adjustu}{Integer vector \code{c(start, end)} (1-based) giving the
rows of \code{var_result$residuals} corresponding to the proxy window.}

\item{indsR1}{Integer 0/1 vector of length \code{adjustu[2]-adjustu[1]+1};
1 = treatment month (e.g. OPEC announcement), 0 otherwise.}

\item{hor}{IRF horizon.}

\item{nvar}{1-based normalization variable index.}

\item{scale}{Shock size for normalization (default 10).}
}
\value{
A list with \code{IRF} (N x hor+1) and \code{b1} (N-vector).
}
\description{
Identifies a structural shock using the external instrument restricted to
high-variance (treatment) months, following the proxy-SVAR approach of
Kaenzig (2021). The structural impact vector is estimated via OLS of the
centred residuals on the centred proxy, restricted to OPEC announcement
months (\code{indsR1 == 1}).
}
