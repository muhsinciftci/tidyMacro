% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fplotirf_bq.R
\name{fplotirf_bq}
\alias{fplotirf_bq}
\title{Plot Blanchard-Quah Long-Run Identified Impulse Response Functions}
\usage{
fplotirf_bq(
  point,
  boot_result,
  varnames,
  shocknames,
  cumulate,
  shocks,
  return_data = FALSE,
  ribbon_fill = "#407EC9",
  ribbon_alpha = 0.2,
  ribbon_alpha2 = 0.35,
  line_color = "#910048",
  zero_line_color = "#707372",
  facet_scales = c("free", "free_y", "free_x", "fixed"),
  facet_ncol = NULL
)
}
\arguments{
\item{point}{Array of point estimates (N x N x horizon+1), typically from
\code{fbqIRF()}. Variables listed in \code{cumulate} will be cumulated
inside this function to recover level responses.}

\item{boot_result}{List returned by \code{fbootstrapBQ()}. Must contain
\code{upper} and \code{lower}. If \code{upper2} and \code{lower2} are
present, a second (inner) confidence band is drawn automatically.}

\item{varnames}{Character vector of variable names (length N).}

\item{shocknames}{Character vector of shock names (length equal to
\code{length(shocks)}).}

\item{cumulate}{Integer vector (1-based) of variable indices whose point
IRFs should be cumulated. Must match the \code{cumulate} argument passed
to \code{fbootstrapBQ()}.}

\item{shocks}{Integer vector (1-based) of shock indices to plot.}

\item{return_data}{Logical, if TRUE returns a tibble instead of a plot.
Default FALSE.}

\item{ribbon_fill}{Color for confidence band fill (default: "#407EC9")}

\item{ribbon_alpha}{Transparency for outer confidence band (default: 0.2)}

\item{ribbon_alpha2}{Transparency for inner confidence band (default: 0.35)}

\item{line_color}{Color for point estimate line (default: "#910048")}

\item{zero_line_color}{Color for horizontal zero line (default: "#707372")}

\item{facet_scales}{Scale option for \code{facet_wrap}: one of
\code{"free"} (default), \code{"free_y"}, \code{"free_x"}, or
\code{"fixed"}.}

\item{facet_ncol}{Number of columns in the facet layout. Default NULL.}
}
\value{
A ggplot object (if \code{return_data = FALSE}) or a tibble.
}
\description{
Creates a grid of IRF plots with shaded confidence bands using ggplot2.
Designed for BQ long-run identified VARs. Supports one or multiple shocks;
when a single shock is supplied the facet shows only response variables.
}
