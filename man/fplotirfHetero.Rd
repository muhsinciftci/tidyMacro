% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fplotirfHetero.R
\name{fplotirfHetero}
\alias{fplotirfHetero}
\title{Plot Heteroskedasticity Bootstrap IRFs with Confidence Bands}
\usage{
fplotirfHetero(
  result,
  varnames,
  shockname,
  scale = 1,
  return_data = FALSE,
  ribbon_fill = "#407EC9",
  ribbon_alpha = 0.2,
  ribbon_alpha2 = 0.35,
  line_color = "#910048",
  zero_line_color = "#707372",
  facet_scales = "free",
  facet_ncol = 2
)
}
\arguments{
\item{result}{List returned by \code{fbootstrapHetero()}. Must contain
\code{point}, \code{upper}, \code{lower}. If \code{upper2} and
\code{lower2} are present, a second (inner) band is drawn.}

\item{varnames}{Character vector of variable names (length N).}

\item{shockname}{Character string for the shock label.}

\item{scale}{Numeric scalar applied to all IRF values. Default 1.}

\item{return_data}{Logical; if \code{TRUE} returns a tibble. Default \code{FALSE}.}

\item{ribbon_fill}{Fill colour for confidence bands. Default \code{"#407EC9"}.}

\item{ribbon_alpha}{Transparency for outer band. Default 0.2.}

\item{ribbon_alpha2}{Transparency for inner band. Default 0.35.}

\item{line_color}{Colour for point estimate line. Default \code{"#910048"}.}

\item{zero_line_color}{Colour for zero reference line. Default \code{"#707372"}.}

\item{facet_scales}{Facet scales: \code{"free"}, \code{"free_y"},
\code{"free_x"}, or \code{"fixed"}. Default \code{"free"}.}

\item{facet_ncol}{Number of columns in facet grid. Default 2.}
}
\value{
A ggplot object (or a tibble if \code{return_data = TRUE}).
}
\description{
Creates a grid of IRF plots for results from \code{fbootstrapHetero()}.
Uses the plug-in point estimate (\code{result$point}) as the center line,
which is the correct center for Hall (1992) recentered confidence bands.
}
