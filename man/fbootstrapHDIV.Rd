% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{fbootstrapHDIV}
\alias{fbootstrapHDIV}
\title{Bootstrap Confidence Bands for the IV Historical Decomposition}
\usage{
fbootstrapHDIV(
  y,
  var_result,
  Z,
  s,
  nboot,
  blocksize,
  adjustZ,
  adjustu,
  policyvar = 1L,
  prc = 90,
  n_threads = 0L
)
}
\arguments{
\item{y}{T x N matrix of original endogenous variables.}

\item{var_result}{List from \code{fVAR}.}

\item{Z}{Instrument matrix aligned to the proxy sample.}

\item{s}{N x 1 unit-normalised point-estimate structural impact vector.}

\item{nboot}{Number of bootstrap replications.}

\item{blocksize}{MBB block size.}

\item{adjustZ}{Integer vector \code{c(start, end)} selecting the proxy-sample
rows of \code{Z} (1-based).}

\item{adjustu}{Integer vector \code{c(start, end)} selecting the proxy-sample
rows of the residuals (1-based).}

\item{policyvar}{Integer (1-based) index of the IV policy variable. Default 1.}

\item{prc}{Confidence level in percent (e.g. 90 for 90\% CI). Default 90.}

\item{n_threads}{OpenMP threads. 0 = all cores minus one. Default 0.}
}
\value{
A list with elements:
  \item{HDshock}{(T-p) x N point-estimate HD matrix.}
  \item{upper}{(T-p) x N upper confidence bands.}
  \item{lower}{(T-p) x N lower confidence bands.}
}
\description{
Computes MBB bootstrap uncertainty bands for the companion-form historical
decomposition of an IV-identified structural shock. In each replication
the VAR is re-estimated, the IV identification is re-run, and the
companion-form HD recursion is applied to the bootstrap draw. Bands are
recentered around the point estimate following Kaenzig (2021).
}
