% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{fBootstrapIVRecover}
\alias{fBootstrapIVRecover}
\title{Bootstrap IRFs for the Recoverable Non-Invertible Case}
\usage{
fBootstrapIVRecover(
  y,
  instr,
  var_result,
  noise,
  delta,
  nboot,
  p,
  c,
  r,
  hor,
  cumu,
  prc = 90,
  prc2 = 68
)
}
\arguments{
\item{y}{T x N matrix of endogenous variables.}

\item{instr}{Length-T raw instrument vector (before cleaning).}

\item{var_result}{List from \code{fVAR}.}

\item{noise}{Length-(T-p-r) noise residuals \eqn{v_t} from the
invertibility regression.}

\item{delta}{Length-\eqn{N(r+1)+1} coefficient vector (intercept + current
and r future VAR residual coefficients) from the invertibility regression.}

\item{nboot}{Number of bootstrap replications.}

\item{p}{VAR lag order.}

\item{c}{Integer (0/1): include intercept.}

\item{r}{Number of future residuals used in the invertibility test.}

\item{hor}{Impulse-response horizon.}

\item{cumu}{1-indexed integer vector of variable positions to cumulate.}

\item{prc}{Primary confidence level (e.g. 90). Default 90.}

\item{prc2}{Secondary confidence level (e.g. 68). Default 68.}
}
\value{
List with N x (hor+1) matrices: upper, lower (at \code{prc}),
  upper2, lower2 (at \code{prc2}), median.
}
\description{
Implements the iid joint-resampling bootstrap for generalized SVAR-IV when
the shock is non-invertible but recoverable (Forni, Gambetti & Ricco 2022).
At each draw the VAR residuals and the instrument noise are resampled jointly,
the instrument is regenerated from the DGP
\eqn{z(t) = \delta(F)e(t) + v(t)}, and \eqn{\Psi(L)} is re-estimated.
}
\seealso{
\code{\link{fBootstrapIVInvertible}}, \code{\link{fGetBands}}
}
