% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{fAICBIC}
\alias{fAICBIC}
\title{Determine Optimal Lag Order for VAR Model Using Information Criteria}
\usage{
fAICBIC(y, pmax, c, exog = NULL)
}
\arguments{
\item{y}{A T x N matrix of endogenous variables (T observations, N series).}

\item{pmax}{An integer specifying the maximum VAR lag order to consider.}

\item{c}{An integer (0 or 1) indicating whether to include a constant term
(1 = include, 0 = exclude).}

\item{exog}{An optional T x M matrix of exogenous variables. Default is
NULL.}
}
\value{
A list with three integer elements: aic — optimal lag length by
  Akaike IC; bic — optimal lag length by Bayesian IC; hq — optimal lag
  length by Hannan-Quinn IC.
}
\description{
Fits VAR models for lag orders 1 through \code{pmax} and returns the lag
length that minimises each of three information criteria.
}
\details{
For each candidate lag order p the residual covariance Sigma = (e'e)/T is
computed and the three criteria are evaluated as:

AIC = log(det(Sigma)) + 2 * (p*N^2 + N + M) / T

BIC = log(det(Sigma)) + log(T) * (p*N^2 + N + M) / T

HQ  = log(det(Sigma)) + 2 * log(log(T)) * (p*N^2 + N + M) / T

where M is the number of exogenous variables.
}
\examples{
\dontrun{
set.seed(123)
y      <- matrix(rnorm(200), ncol = 2)
result <- fAICBIC(y, pmax = 10, c = 1)
result$aic
result$bic
}

}
\seealso{
\code{\link{fVAR}}
}
