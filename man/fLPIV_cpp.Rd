% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{fLPIV_cpp}
\alias{fLPIV_cpp}
\title{Local Projections with External Instrument — C++ Engine}
\usage{
fLPIV_cpp(
  Y,
  D,
  Z,
  C,
  H,
  conf_level,
  nw_lags_iv,
  cumulative = FALSE,
  n_threads = 0L,
  verbose = FALSE
)
}
\arguments{
\item{Y}{Numeric matrix (T x n_y). Outcome variable(s).}

\item{D}{Numeric vector (T). Endogenous treatment.}

\item{Z}{Numeric matrix (T x n_z). External instrument set — build any
lags externally (e.g. via \code{l(z, 0:k)} in the wrapper).}

\item{C}{Numeric matrix (T x n_c). Exogenous controls; NO intercept (the
engine adds one internally). May be empty (0 columns).}

\item{H}{Integer. Maximum horizon (h = 0, 1, ..., H).}

\item{conf_level}{Numeric. Confidence level for bands, e.g. 0.90.}

\item{nw_lags_iv}{Integer. Newey-West Bartlett bandwidth for the IV score.
\code{> 0}: fixed bandwidth (Stata's \code{vce(hac nw <k>)});
\code{== 0}: horizon-varying bandwidth = h (just-identified LP rule).}

\item{cumulative}{Logical. \code{TRUE} regresses \eqn{y_{t+h} - y_{t-1}};
\code{FALSE} regresses the level \eqn{y_{t+h}}.}

\item{n_threads}{Integer. OpenMP threads. \code{0} = all cores minus one.}
}
\value{
A named list with \code{irfs}, \code{irfs_upper}, \code{irfs_lower},
  \code{irfs_se}, \code{Fstat_fs} (first-stage F per horizon) and
  \code{rsqr_fs} (first-stage R^2 per horizon).
}
\description{
Low-level engine called by \code{fLPIV()}. Prefer the high-level wrapper.
}
\keyword{internal}
