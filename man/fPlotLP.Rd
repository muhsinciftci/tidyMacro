% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fPlotLP.R
\name{fPlotLP}
\alias{fPlotLP}
\title{Plot Local Projection Impulse Responses}
\usage{
fPlotLP(
  x,
  variables = NULL,
  scale = 1,
  return_data = FALSE,
  ribbon_fill = "#407EC9",
  ribbon_alpha = 0.2,
  line_color = "#910048",
  zero_line_color = "#707372",
  facet_scales = "free_y",
  facet_ncol = NULL
)
}
\arguments{
\item{x}{An object of class \code{"fLP"} returned by \code{fLP()}.}

\item{variables}{Optional character vector of response variables to plot.
Defaults to all responses.}

\item{scale}{Numeric scalar applied to IRFs and confidence bands before
plotting. Default is 1.}

\item{return_data}{Logical. If \code{TRUE}, return the plotting data
(long format when multi-band) instead of a ggplot object.}

\item{ribbon_fill}{Color for the confidence band fill.}

\item{ribbon_alpha}{Transparency for the confidence band. For multi-band
fits, either a single number (interpreted as the alpha of the widest band;
inner bands get proportionally darker) or a numeric vector of the same
length as the confidence levels, matched widest-to-tightest.}

\item{line_color}{Color for the IRF line.}

\item{zero_line_color}{Color for the horizontal zero line.}

\item{facet_scales}{Facet scales option: \code{"free_y"}, \code{"free"},
\code{"free_x"}, or \code{"fixed"}.}

\item{facet_ncol}{Number of facet columns. Defaults to an automatic layout.}
}
\value{
A ggplot object, or a tibble if \code{return_data = TRUE}.
}
\description{
Plots impulse responses and confidence bands from an object returned by
\code{fLP()}. Handles both single-band and multi-band fits transparently:
if \code{fLP()} was called with \code{conf = c(0.68, 0.95)} the plot
automatically draws one ribbon per level (widest ribbon is drawn first with
lowest opacity so the tighter band is visually on top).
}
